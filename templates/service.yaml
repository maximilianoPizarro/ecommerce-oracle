apiVersion: v1
kind: Service
metadata:
  name: {{ include "workshop-pipelines.fullname" . }}
  labels:
    {{- include "workshop-pipelines.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workshop-pipelines.selectorLabels" . | nindent 4 }}
