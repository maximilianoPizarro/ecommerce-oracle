apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workshop-pipelines.fullname" . }}-test-connection"
  labels:
    {{- include "workshop-pipelines.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "workshop-pipelines.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
